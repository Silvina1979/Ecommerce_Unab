name: Ecommerce CI/CD

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]

jobs:
  backend:
    name: Backend - Build & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "21"  # ✅ Cambié de 17 a 21

      - name: Cache Maven
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build Backend
        working-directory: Backend
        run: ./mvnw -B clean package

      - name: Run Backend Tests
        working-directory: Backend
        run: ./mvnw test -DskipTests=false  # ✅ Fuerza tests (override pom.xml)

      - name: Upload Backend Artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: Backend/target/*.jar

  frontend:
    name: Frontend - Lint, Test & Build
    runs-on: ubuntu-latest
    needs: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Dependencies
        working-directory: Frontend
        run: npm ci

      - name: Lint Frontend
        working-directory: Frontend
        run: npm run lint --if-present

      - name: Run Frontend Tests
        working-directory: Frontend
        run: npm run test --if-present

      - name: Build Frontend
        working-directory: Frontend
        run: npm run build

      - name: Upload Frontend Artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: Frontend/dist/

  # ✅ OPCIONAL: Descomenta si querés deploy automático
  # deploy:
  #   name: Deploy Production
  #   needs: [backend, frontend]
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'  # Solo en main
  #   steps:
  #     - name: Deploy Backend to Render
  #       run: |
  #         curl https://api.render.com/deploy/srv-xxxxx?key=${{ secrets.RENDER_DEPLOY_KEY }}
  #     - name: Deploy Frontend to Vercel
  #       run: |
  #         npm install -g vercel
  #         vercel --prod --token ${{ secrets.VERCEL_TOKEN }}
