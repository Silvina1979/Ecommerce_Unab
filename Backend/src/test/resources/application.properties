# --- CONFIGURACIÓN  PARA TESTS ---

# Base de datos en memoria H2
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Claves FALSAS (Solo para que @Value no tire error al levantar)
resend.api.key=test_key
mp.access.token=test_token
jwt.secret.key=test_secret_key_super_larga_para_que_no_falle_validacion_123456
cloudinary.cloud_name=test
cloudinary.api_key=test
cloudinary.api_secret=test

# URLs Dummy
app.backend.url=http://localhost
app.frontend.url=http://localhost